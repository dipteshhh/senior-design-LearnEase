PORT=3001
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_TIMEOUT_MS=30000
OPENAI_MAX_RETRIES=2
GOOGLE_CLIENT_ID=your_google_oauth_client_id_here
GOOGLE_TOKENINFO_TIMEOUT_MS=8000
GOOGLE_TOKENINFO_MAX_RETRIES=1
DATABASE_PATH=data/learnease.sqlite
ARTIFACTS_DIR=data/artifacts
RETENTION_DAYS=30
RATE_LIMIT_MAX=10
SESSION_MAX_AGE_SECONDS=604800
FILE_ENCRYPTION_KEY=replace_with_64_char_hex_key
SESSION_SECRET=replace_with_strong_random_secret
# Test-only: ignored unless NODE_ENV=test.
ALLOW_LEGACY_AUTH_COOKIES=false
CORS_ORIGINS=http://localhost:3000
LOG_LEVEL=info
# Production only: configure Express trust proxy for correct IP-based rate limiting behind reverse proxies.
# Accepted values: true (trust all), false (disabled), number (hop count), or IP/subnet string.
# TRUST_PROXY=true
